{
  "name": "makerperks-mcp",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "First-party read-only MCP server over the MakerPerks builder-perks directory (Cloudflare Worker).",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "build": "wrangler deploy --dry-run --outdir dist"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "agents": "^0.1.0",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250109.0",
    "typescript": "^5.8.2",
    "wrangler": "^4.105.0"
  }
}
