{
  "name": "makerperks-engagement-worker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Capture substrate: anonymous, deduplicated, bot-resistant engagement state at the edge (Cloudflare Worker + D1). Records favorite/redeem events and serves identity-deduplicated aggregate counts.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "test": "node --experimental-strip-types --test src/handler.test.ts",
    "build": "wrangler deploy --dry-run --outdir dist"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250109.0",
    "typescript": "^5.8.2",
    "wrangler": "^4.105.0"
  }
}
